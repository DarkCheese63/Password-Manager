# Password-Manager
This is a password manager written in python and uses SQL for the database.
Uses fernet symmetric encryption to encrypt and decrypt passwords. Used for storing the passwords securely on the local machine.
